<p><%= link_to "Neuer Spielort", { :action => :new }, { :class => "btn btn-primary" } %></p>
<table class="table">
  <% @locations.each do |location| %>
    <tr>
      <td><%= location.name %></td>
      <td>
        <div class="btn-group">
	  <%= link_to 'Bearbeiten', edit_admin_location_path(location), :class => "btn btn-primary" %>
          <%= link_to 'Löschen', [:admin, location], :method => :delete, :class => "btn btn-danger" %>
	</div>
      </td>
    </tr>
  <% end %>
</table>
<p><%= link_to "Neuer Spielort", { :action => :new }, { :class => "btn btn-primary" } %></p>

