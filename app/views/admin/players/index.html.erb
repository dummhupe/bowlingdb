<p><%= link_to "Neuer Spieler", { :action => :new }, { :class => "btn btn-primary" } %></p>
<table class="table">
  <% @players.each do |player| %>
    <tr>
      <td><%= player.full_name %></td>
      <td>
        <div class="btn-group">
	  <%= link_to 'Bearbeiten', edit_admin_player_path(player), :class => "btn btn-primary" %>
          <%= link_to 'Löschen', [:admin, player], :method => :delete, :class => "btn btn-danger" %>
	</div>
      </td>
    </tr>
  <% end %>
</table>
<p><%= link_to "Neuer Spieler", { :action => :new }, { :class => "btn btn-primary" } %></p>
